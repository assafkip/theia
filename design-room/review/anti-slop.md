# Floor — anti-slop (reactive gate — fires in Phase 6, pre-registered here)

Constraints the build must clear (from the anti-AI gate + this buyer's allergy):

- NO gradient-text headline, NO glassmorphism, NO purple-blue AI-startup gradient
  hero. This buyer reads that as "marketing box, no receipts" (decision-psychology).
- NO emoji icons. The MVP uses ⚠/↻ inline — replace with real marks or restrained
  glyphs. Both references use zero emoji.
- NO generic "Powerful. Fast. Secure." feature-triplet copy. Copy must be specific
  and grounded (source: founder's words / the real mechanic).
- SHOW the situation, don't name the feeling: don't write "trustworthy" — show the
  dropped-count and the source-span. The receipts ARE the trust claim.
- NO convergent library-default look (the LEAD's `bespoke-vs-default` gap). At least
  one made-for-this-page element.
- Mono must be a REAL mono face for evidence surfaces (steal-manifest `mono-for-evidence`),
  not a proportional font faking code.
